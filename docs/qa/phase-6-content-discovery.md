# 第六阶段：内容发现与项目页 QA 记录

- 测试日期：2026-09-22
- 功能提交：`68d5b50`
- 分支：`codex/phase-6-content-discovery-20260922`
- 生产预览：Astro `preview`，本地地址 `http://127.0.0.1:4325`

## 自动化验证

| 命令 | 结果 |
| --- | --- |
| `npm run verify` | 通过；内容发现测试 5/5、工具测试 65/65 |
| `npm run astro -- check` | 通过；0 errors、0 warnings、0 hints |
| `npm run build` | 通过；生成 29 个页面 |

构建产物包含项目列表与详情、归档页、标签索引、9 个标签详情、3 篇文章详情，以及既有文章、专题、工具和关于页面。

## 桌面端检查

- 视口：`1440 x 900`
- 路由：`/projects/`、`/projects/bigbrotherwei-github-io/`、`/archive/`、`/tags/`、全部 9 个标签详情、全部 3 篇文章详情、`/posts/`、`/about/`
- 背景：项目、归档、标签索引和标签详情均加载对应横版手绘背景；图片清晰且正文对比度合格。
- 布局：未发现卡片嵌套、内容重叠或横向溢出；标签正常换行，文章上一篇/下一篇方向正确，相关推荐可读。
- 外链：项目详情的“查看仓库”和“访问网站”均使用 `target="_blank"` 与 `rel="noreferrer"`。
- 控制台：0 errors、0 warnings。

## 移动端检查

- 视口：`390 x 844`
- 路由：项目列表与详情、归档、标签索引、`GitHub Pages` 标签详情、文章详情、文章列表、关于页面。
- 背景：关键页面均使用 `-mobile.webp` 竖版背景。
- 布局：固定导航未遮挡正文；`GitHub Pages`、`GitHub Actions` 等长标签可换行；文章相邻导航纵向排列；页面无横向滚动。
- 控制台：0 errors、0 warnings。

## 键盘与边界状态

- 用键盘触发代表性导航链接焦点，`:focus-visible` 生效，显示 `3px` 浅色轮廓和 `3px` 偏移。
- 内容发现单元测试覆盖：标签 slug 冲突、归档排序与草稿排除、单篇文章的相邻边界、无相关推荐与草稿排除。
- 静态验证覆盖文章子导航、标签链接、相邻文章、相关推荐以及项目外链的关键结构。

## 缺陷与环境说明

- QA 未发现需要修改产品代码的缺陷。
- 沙箱环境禁止普通进程监听本地端口，授权后启动生产预览完成浏览器检查；此限制不影响构建产物和 GitHub Pages 部署。
